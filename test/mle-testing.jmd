
```julia; echo = false; results = "hidden"
using Distributions
using OrdinaryDiffEq
using BenchmarkTools
# using Plots
# using StatsPlots

using Revise
include("../src/DEParamDistributions.jl")
using .DEParamDistributions
```

## Constructing a DEModel object

Trying out a new struct that should remove always passing param distributions and likelihoods.

```julia
ptrue = SIRParamDistribution(30., 0.4, 0.5, 0.1)
pdist = SIRParamDistribution(30., Beta(1, 1.3), TruncatedNormal(1, 2, 0, 5), 0.1)
prob = ode_problem(ptrue; save_idxs=1, saveat=1.)
inf_curve = solve(prob, Tsit5()).u
y = rand.(Binomial.(20, inf_curve))

mle(y, pdist, x->DEParamDistributions.joint_binom(20, x))
```