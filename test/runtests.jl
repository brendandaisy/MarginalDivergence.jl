# TODO: test odd cases for marginal divergence: no observations, single ODE timepoint (such as only initial conditions)

include("test-latent.jl")
include("test-observation.jl")
# include("test-inference.jl")